<component>
	<previews>
		<preview name="default" width="320" height="20" />
	</previews>

	<!-- A 1px full-width divider line. No API — drop it between rows wherever
	     you want a visual break. Extends the theme-free container, so the
	     bg_color/opa/height locals apply after remove_style_all and stick. -->
	<view extends="container" width="100%" height="1"
	      style_bg_color="#color_track" style_bg_opa="25%" />
</component>
